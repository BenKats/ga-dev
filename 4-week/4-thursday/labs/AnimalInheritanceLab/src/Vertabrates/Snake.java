package Vertabrates;

public class Snake extends Reptile {
    public boolean shedSkin = true;

    public Snake(String name, boolean isEndangered, int topSpeed) {
        super(name, isEndangered, topSpeed);
    }
}
